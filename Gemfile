source 'https://rubygems.org'
gem 'fastlane'
gem 'xcodeproj'
# fastlane's google-apis chain lazily requires multi_json but doesn't declare it
# as a dependency; without this the bundle errors on load under Ruby 3.3+.
gem 'multi_json'
