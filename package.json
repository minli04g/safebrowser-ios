{
  "name": "safebrowser-ios",
  "version": "0.1.0",
  "private": true,
  "description": "SafeBrowser parent iOS shell (Capacitor) — loads the remote dashboard + APNs push",
  "scripts": {
    "sync": "cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^8",
    "@capacitor/ios": "^8",
    "@capacitor/push-notifications": "^8"
  },
  "devDependencies": {
    "@capacitor/cli": "^8",
    "typescript": "^5"
  }
}
